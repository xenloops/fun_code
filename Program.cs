// Example usage
class Program
{
    static void Main()
    {
        DateTime today = AstronomicalChecker.IsFullMoon(today);
        Console.WriteLine($"Is today a full moon? {isFullMoon}");
    }
}
